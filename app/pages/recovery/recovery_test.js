'use strict';

describe('myApp.recovery module', function() {

  beforeEach(module('myApp.recovery'));

  describe('recovery controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var recoveryCtrl = $controller('recoveryCtrl');
      expect(recoveryCtrl).toBeDefined();
    }));

  });
});