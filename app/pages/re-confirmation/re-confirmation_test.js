'use strict';

describe('myApp.re-confirmation module', function() {

  beforeEach(module('myApp.re-confirmation'));

  describe('re-confirmation controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var re-confirmationCtrl = $controller('re-confirmationCtrl');
      expect(re-confirmationCtrl).toBeDefined();
    }));

  });
});`