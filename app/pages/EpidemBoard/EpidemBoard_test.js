'use strict';

describe('myApp.EpidemBoard module', function() {

  beforeEach(module('myApp.EpidemBoard'));

  describe('EpidemBoard controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var EpidemBoardCtrl = $controller('EpidemBoardCtrl');
      expect(EpidemBoardCtrl).toBeDefined();
    }));

  });
});