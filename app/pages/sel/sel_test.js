'use strict';

describe('myApp.sel module', function() {

  beforeEach(module('myApp.sel'));

  describe('sel controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var selCtrl = $controller('selCtrl');
      expect(selCtrl).toBeDefined();
    }));

  });
});`